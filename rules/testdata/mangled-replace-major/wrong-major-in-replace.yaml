name: a replace rewritten to the wrong major is closed
subject:
  state: failed
  kind: renovate
  title: "fix(deps): update module github.com/giantswarm/mcp-oauth to v5.3.1"
  failing: ["go-build"]
  logs:
    "actions:go-build": |
      go: errors parsing go.mod:
      go.mod:42: replace github.com/giantswarm/mcp-oauth/v4: version "v5.3.1" invalid: should be v4, not v5
expect:
  rule: mangled-replace-major
