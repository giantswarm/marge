name: a chart push that lost the race on the shared catalog is retried
subject:
  state: failed
  kind: renovate
  title: "chore(deps): update giantswarm/architect orb to v10.5.1"
  failing: ["ci/circleci: push-chart"]
  logs:
    "circleci:ci/circleci: push-chart": |
      ====> Attempt 4: Running: git commit -m "add giantswarm-platform-manager-0.1.0-dev.tgz"
      ====> Attempt 4: Running "git push"
      To github.com:giantswarm/giantswarm-test-catalog.git
       ! [rejected]          master -> master (fetch first)
      error: failed to push some refs to 'github.com:giantswarm/giantswarm-test-catalog.git'
      hint: Updates were rejected because the remote contains work that you do not
      hint: have locally. This is usually caused by another repository pushing to
      hint: the same ref.
      Giving up after 4 failures.
      Error pushing changes. See known errors in:
      https://github.com/giantswarm/architect-orb/blob/master/README.md#push-to-app-catalog
expect:
  rule: circleci-catalog-push-race
  check: "ci/circleci: push-chart"
